{
  "name": "oakridgemun-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Local runnable copy of the public Oakridge MUN front end.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "start": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  }
}
